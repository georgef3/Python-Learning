#6.5 Write code using find() and string slicing (see section 6.10) to extract the number at the end of the line below. 
#Convert the extracted value to a floating point number and print it out.

text = "X-DSPAM-Confidence:    0.8475";
#1. count the length of the text noted above
my_length=len(text)
#print my_length

#2. find the first 0 in the string and equates its place to 'zerobegin'
zerobegin=text.find('0')
#print zerobegin

#3. creates a variable string target after the first 0 through the length of the string
stringtarget=text[zerobegin+1:my_length]
#print stringtarget

#4. floats the stringtarget variable from text to an integer with a decimal point
intstringtarget=float(stringtarget)
print intstringtarget
